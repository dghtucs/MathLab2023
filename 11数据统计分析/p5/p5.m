%炮弹射击
% 定义参数
mu = [0 0]; %均值，目标中心
sigma = [80^2, 80*50*0.4; 80*50*0.4, 50^2]; %协方差矩阵
r = 100; % 圆形区域半径

% 定义二维正态分布
pd = mvnrnd(mu, sigma, 1e6);

% 计算点到圆心的距离
d = sqrt(pd(:,1).^2 + pd(:,2).^2);

% 计算在圆形区域内的点的数量
in_circle = sum(d <= r);

% 计算炮弹落在圆形区域内的概率
prob = in_circle / 1e6;

disp(prob)
